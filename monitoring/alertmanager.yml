route:
  receiver: team-webhook
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h

receivers:
  - name: team-webhook
    webhook_configs:
      - url: http://alert-receiver:5000/alerts
        send_resolved: true
